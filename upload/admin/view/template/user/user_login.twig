<div class="table-responsive">
  <table class="table table-bordered table-hover">
    <thead>
      <tr>
        <td class="text-left">{{ column_ip }}</td>
        <td class="text-left">{{ column_user_agent }}</td>
        <td class="text-right">{{ column_date_added }}</td>
      </tr>
    </thead>
    <tbody>
      {% if logins %}
        {% for login in logins %}
          <tr>
            <td class="text-left"><a href="https://whatismyipaddress.com/ip/{{ login.ip }}" target="_blank">{{ login.ip }}</a></td>
            <td class="text-left">{{ login.user_agent }}</td>
            <td class="text-right">{{ login.date_added }}</td>
          </tr>
        {% endfor %}
      {% else %}
        <tr>
          <td class="text-center" colspan="3">{{ text_no_results }}</td>
        </tr>
      {% endif %}
    </tbody>
  </table>
</div>
<div class="row">
  <div class="col-sm-6 text-left">{{ pagination }}</div>
  <div class="col-sm-6 text-right">{{ results }}</div>
</div>